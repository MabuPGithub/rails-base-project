<div class="pending-users-container">
  <div class="back-container">
    <%= link_to "◄ Back", "/admins" %>
  </div>
<h1 id="pending-h1">Pending Users</h1>
<% @users.each do |user| %>
  <div class="users-item-container">
    <p>Email: <strong><%= user.email %></strong></p>
    <p>Name: <strong><%= user.name %></strong></p>
    <% if user.approved == false %>
        <p>Status: <strong>Pending</strong></p>
    <% else %>
        <p>Status: <strong>Approved</strong></p>
    <% end %>
    <p><%= button_to "Approve User", admin_pending_approve_path,
    params: {:id => user.id, :approved => user.approved},
    method: :post %></td></p>
  </div>
<% end %>
</div>